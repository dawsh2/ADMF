system:
  name: "ADMF-Trader-MVP"
  version: "0.1.0"

logging:
  level: "INFO" # We'll use this when we set up logging
  # In the future, we might add:
  # file_output: false
  # file_path: "admf_trader.log"

# Placeholder for data settings, might be used by DataHandler in Phase 1
data:
  csv_file_path: "data/SPY_1min.csv" # Assuming your SPY_1min.csv is in a 'data' folder at the rooty